<% if @post.errors.any? %>
<%= pluralize(@post.errors.count, "error") %>pfohibited this object from being saved: not successfully
<% @post.errors.full_messages.each do |message| %>
<%= message %>
<% end %>
<% end %>

<%= form_for @post do |f| %>

  <%= f.label :content, "積み上げ内容：" %>
  <%= f.text_field :content %>

  <%= f.submit %>
<% end %>
